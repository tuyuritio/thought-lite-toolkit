---
"@tuyuritio/rehype-image-figure": major
---

Introduced `rehype-image-figure` to convert root-level paragraphs containing a single image into semantic `<figure>` elements with captions derived from the image's alt text.
